matrix:
  include:
    - os: linux
      env:
        - BUILD_TARGET=android_cmake
      language: android
      sudo: required
    - os: linux
      env: 
        - BUILD_TARGET=linux 
      language: cpp
      sudo: required
      

before_install:
  - travis-scripts/before-install.sh

script:
  - travis-scripts/run-script.sh

cache: 
  bundler: true